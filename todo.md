# Todo

- Get quantiles directly from WARp predictions (rather than having to use `dens2quantile`)
- Organize code better by using more files and potentially classes
- Figure out what type of model ftsm's `forecast` uses (what order)
- Figure out a better way of testing models
